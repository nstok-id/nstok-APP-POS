/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  experimental: {
    // Allows accessing dev server from mobile devices (iOS/Android) on local LAN
  },
};

module.exports = nextConfig;
